import React from "react";
import {Image} from "react-native";

export default function Visa(props) {
    return (
        <Image {...props} source={require("../../assets/visa.png")}>
        </Image>
    )
}
